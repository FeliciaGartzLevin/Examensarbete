import React, { ReactNode } from 'react'

type AutocompleteProps = {
	children: ReactNode
}

export const Autocomplete: React.FC<AutocompleteProps> = ({ children }) => {
	return (
		<div>Autocomplete</div>
	)
}

