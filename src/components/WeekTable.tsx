import React, { ReactNode } from 'react'

type WeekTableProps = {
	children: ReactNode
}

export const WeekTable: React.FC<WeekTableProps> = ({ children }) => {
	return (
		<div>WeekTable</div>
	)
}

